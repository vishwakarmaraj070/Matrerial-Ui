import {
    compose,
    spacing,
    borders,
    display,
    flexbox,
    positions,
    shadows,
    sizing,
    typography,
    palette
} from "@material-ui/system";

export const BoxSystem = compose(
    spacing,
    borders,
    display,
    flexbox,
    positions,
    shadows,
    sizing,
    typography,
    palette
);
